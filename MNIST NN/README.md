# MNIST Neural Nets

These are two separate Google Colab notebooks, both of which are running MNIST classification. The difference lies in that one is running a Convolutional Neural Network, while the other is a Simple Neural Network.
